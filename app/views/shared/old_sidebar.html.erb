    <div class="panel panel-default">
      <div class="panel-heading"><%= current_user.name %></div>
      <div class="panel-body">
        <% if current_user.admin? %>
          <%= link_to t('business_unit'), chatrooms_path %>
        <% else %>
          <%= t('business_unit') %>
        <% end %>
        <ul class="sidebar-list">
          <ul>
            <% current_user.chatrooms.public_channels.each do |chatroom| %>
              <li><%= link_to chatroom.display_name, chatroom, data: {behavior: "chatroom-link", chatroom_id: chatroom.id} %></li>
            <% end %>
          </ul>
<!--
          <div class="row">
            <div class="col-md-offset-4 col-md-4">
              <button id="filter" class="btn btn-default" data-toggle="collapse" data-target="#ChatPanel">               
                <i class="material-icons md-24">phone</i>
              </button>
            </div>
          </div>

          <div class="collapse" id="ChatPanel">
            <ul>
              <% (User.all - [current_user]).sort_by(&:username).each do |user| %>
              <li>
                <%= image_tag user_avatar(user, 30), class: "rounded-circle" %>
                <% if user.online? %>
                  <%= Emoji.find_by_alias("white_check_mark").raw %>
                <% else %>
                  <%= Emoji.find_by_alias("no_entry").raw %>
                <% end %> 
                <%= link_to user.username, direct_message_path(user.id),
                   data: {behavior: "chatroom-link",
                   chatroom_id: Chatroom.direct_message_for_users([current_user, user]).id } %></li>
              <% end %>
            </ul>
          </div>
-->
        </ul>
      </div> 
    </div>